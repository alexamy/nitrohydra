# nitrohydra

## Project
Rust binary crate (`edition = "2024"`), currently in early development.

## Build & Run
```bash
cargo build
cargo run
cargo test
cargo clippy
```

## Notes
- Rust 2024 edition — uses the latest edition idioms
- GUI: `eframe` 0.30 (wraps `egui`). Access egui via `use eframe::egui`
